server:
  port: 9100

spring:
  application:
    name: provider-order
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:11000
      profile: dev

management:
  endpoint:
    shutdown:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

data:
  name: NaN